var dateToObj = function (dateString) {
  // 2019-11-01
  var year = dateString.slice(0, 4);
  var month = dateString.slice(5, 7);
  var day = dateString.slice(8, 10);

  return {
    year: year,
    month: month,
    day: day
  }
}
module.exports = {
  dateToObj: dateToObj,
};
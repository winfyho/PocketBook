<view class="day">
  <!-- <view class="day-num">18号</view>
  <view class="item">
    <text class="time">21:21</text>
    <text class="place">地点</text>
    <text class="detail">备注信息</text>
  </view> -->
  <!-- <image src=""> </image>
  <editor show-img-size show-img-toolbar show-img-resize	/> -->
  <!-- <button bindtap="getMess">订阅消息</button> -->

  <!-- <sche-item wx:for="{{scheduleList}}" wx:key="{{index}}" detail="{{item.detail}}" /> -->


  <sche-day wx:for="{{dayList}}" wx:key="{{index}}" dayItem="{{item}}" dateObj="{{item.dateObj}}" />


  <view class="add-btn">
    <button bindtap="showEditor">记</button>
  </view>
</view>

<sche-editor class="editor-com" wx:if="{{editActive}}" bind:emit="closeEditor" bind:edit="editComplete" />
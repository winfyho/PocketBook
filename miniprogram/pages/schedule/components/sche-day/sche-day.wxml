<view class="sche-day">

  <view class="date">
    <text class="month">{{month}}月</text>

    <text class="day">{{day}}</text>
  </view>
  <view class="day-item">
    <sche-item  wx:for="{{records}}" wx:key="{{index}}" detail="{{item}}" item_id="{{item._id}}" />

  </view>

</view>